function printTable(n) {
    let output = '';
    for (let i = 1; i <= 10; i++) {
      output += (n * i) + ' ';
    }
    console.log(output);
  }
  
  
  printTable(3); 
  printTable(5); 
